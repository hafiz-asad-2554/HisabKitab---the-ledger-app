node_modules
.expo

# Generated build artifacts — contain machine-specific absolute paths (Bug 5)
android/build
android/app/build
android/.gradle

# Native directories regenerated by `npx expo prebuild --clean`
# Keep android/ and ios/ committed (bare workflow), but exclude build outputs above
